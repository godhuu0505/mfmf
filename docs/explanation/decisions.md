# 設計決定ログ（D ログ）

**このリポジトリで唯一「維持する価値がある」ドキュメント。**
現状の仕様（画面一覧・機能一覧・データモデル）は AI にコードを読ませれば数秒で分かるので書きません。
一方、**採用しなかった案とその理由はコードに痕跡が残らない**ため、ここにだけ残します。

なぜこの方針なのかは [prototype-first.md §4-5](./prototype-first.md) を参照。

## 書き方（守るのはこれだけ）

- **1 決定 = 1 行。** 長く書かない。長い説明が要るなら別ドキュメントに切り出してリンクする
- **却下した案とその理由を必ず含める。** これが本ログの存在理由。「Xを採用」だけでは半分の価値しかない
- 番号は `D<n>` の通し番号。**欠番にしない・振り直さない**（commit 件名から参照されるため）
- commit 件名で `Dx` を引用する既存の慣習を続ける（例: `feat: S4 ゲスト閲覧/記入 UI（UC-G02/G03・D4/UC-S01）`）
- **実装しなかった決定こそ書く。** 実装 PR が発生しないので忘れやすいが、価値は最も高い

## D1〜D14（Phase 3.5）

Phase 3.5 の設計決定は
[phase-3-5-use-cases.md §3 設計決定ログ](./phase-3-5-use-cases.md#3-設計決定ログ本バックログの前提)
にあります。既に多数の commit から参照されているため、**移動せずその場に残します**。

要約: D1 ユーザー×複数世帯は採用 / D2 世帯切替 UI は S3 と同時 / D3 外部は Google 非依存を許容 /
D4 匿名リンク閲覧は廃止 / D5 editor は他人の記録も編集可 / D6 最後の owner は降格・退出不可 /
D7 セルフサインアップは新世帯作成と招待受諾の両導線 / D8 ゲストは既定 deny /
D9〜D14 は原典を参照。

## D15 以降（フェーズ非依存）

| # | 決定 | 却下した案とその理由 |
| --- | --- | --- |
| **D15** | 新機能の画面検討は **`proto/<slug>` 使い捨てブランチの実コードプロトタイプ**で行い、合意後にブランチを捨てる。リポジトリに成果物を残さず、永続化するのは本ログ 1 行だけ | **`mocks/` に静的 HTML を置く案を却下**。既存コンポーネントが育った今のリポジトリでは実装プロトの方が速く（`gallery/page.tsx` は 99 行中約半分がデータ取得＝固定配列に差し替えれば 50 行）、実機 PWA の触り心地が測れず、実装時に全捨てになり、7 個目の陳腐化ドキュメントになるため。詳細は [prototype-first.md §2](./prototype-first.md) |
| **D16** | ドキュメントは**コードから復元できないものだけ**残す。現状の仕様は書かない・維持しない | **全ドキュメントを最新に保つ案を却下**。1 日 10〜16 commit の速度では手動同期は構造的に飛ばされる。実際 14 箇所が陳腐化し、腐ったのは全て「誰も読まないもの」だった。ただし `CLAUDE.md` / `AGENTS.md` の誤りは全セッションに誤情報を注入するため**バグとして扱い即修正する** |
| **D17** | エージェントに対する強制力は **`.claude/settings.json` の `deny` / `allow`** で表現する。`guard.mjs` は「値の中身に依存する漏洩」（env / 鍵 / `service_role`）だけを見る | **「危険なコマンドの形をフックで列挙して塞ぐ」案を却下**。`git worktree remove --force` を止める判定を実際に書いたところ、22 巡かけても抜け道が尽きず（語順・省略形・引用符・ANSI-C・行継続・サブシェル・`env -S`・`bash -c` …）、**その約半分は前巡の修正自身が作った穴**で、過剰ブロックも 4 回出た。変数展開・コマンド置換・`eval` は静的に見抜けないので原理的に閉じない。一方 `deny` は正規形を確実に拒否し、`allow` に無いものは必ず承認プロンプトになる —— **プロンプトは最初から確実に効く / ガードは 22 巡かけても収束しない** |

## D18 以降（Phase 3.5 以前からの決定 / 2026-07-30 に本ログへ集約）

もとは `design-decisions.md` にあったもの。D16 に従い、**現状の仕様の記述は捨てて「なぜ」だけ**を移しました。
（同ファイルの「共有方針 (A) 1 アカウント共用」「RLS は `owner_id = auth.uid()`」は D1 の世帯モデル移行で
**失効済み**のため移していません。現在の正は `CLAUDE.md` と `supabase/migrations/`）

| # | 決定 | 却下した案とその理由 |
| --- | --- | --- |
| **D18** | 配信は Vercel Hobby、認証/DB/画像は Supabase Free に寄せて**無料枠で完結**させる | **ネイティブアプリ化と独自サーバーを却下**。少人数で使う PWA に運用コストと運用対象を増やす価値がない |
| **D19** | フェーズ1 は「記録（テキスト＋写真）を残して振り返る」核だけを作る | **LINE 自動取り込み / カレンダー連携 / Google ドライブ・フォト連携 / プッシュ通知を意図的に外した**。やりたいことを増やすより、まず日々続けられる最小形を優先するため |
| **D20** | 画像は**ブラウザ側**で長辺 1600px へ縮小・JPEG 再圧縮してからアップロードする（`src/lib/imageResize.ts`）。PWA アイコンは Node 標準 zlib で直接生成（`npm run icons`） | **サーバー側リサイズを却下**（Free 枠の帯域と Vercel Function ボディ上限 4.5MB に収めるため）。**画像処理ライブラリの追加も却下**（アイコン生成のためだけに依存を増やさない） |
| **D21** | ご意見・不具合フォームの内容は **Supabase の `feedback` テーブルにのみ**保存する | **公開リポジトリへの自動 Issue 起票を却下**。フォームには個人的な内容が入りうる。Issue 化は必要なときだけ、マスクして**非公開リポジトリ宛て**に手元スクリプトで転記する（[feedback-to-issues.md](../guides/feedback-to-issues.md)） |
| **D22** | デプロイは **GitHub Actions から Vercel CLI で明示的に**行い、Vercel の Git 連携自動デプロイは無効化する（`vercel.json` の `git.deploymentEnabled: false`）。main マージ = 本番リリース | **Vercel の Git 連携による自動デプロイを却下**（「いつ・どこへ出すか」を Actions に一本化するため）。**preview 用の別 Supabase プロジェクトも却下**（Free 枠の active 2 プロジェクト上限と両立しない。Database Branching は Pro 以上）。Free には自動バックアップも PITR も無いため `migrate` は push 前に本番 DB をダンプして Artifact に 90 日保存する —— **これが唯一のロールバック資産**で、Storage の写真は対象外（**写真の事故からは戻せないことを許容**） |
| **D23** | Service Worker は Supabase の API レスポンスと署名付き写真 URL を**キャッシュしない** | **全レスポンスをキャッシュして高速化する案を却下**。署名付き URL は private かつ期限付きで、期限切れ URL や他人に見えてはいけない内容がキャッシュに残るため。静的アセットは stale-while-revalidate、ページ遷移は network-first に留める |

## 未決事項

決めていないことも、**決まっていないと分かる形で**残す（`phase-3-5-use-cases.md` §7 と同じ役割。
半年後に読み返して決着させるのはこの節）。

| # | 論点 | 現状 | 決めるべきこと |
| --- | --- | --- | --- |
| **O1** | `/login` の「Google でログイン」ボタンを、Google が使えない構成で出し分けるか | **常に表示している。** メール/パスワードのみの構成では押すと `/login?error=oauth` に戻る（README のクイックスタートに注記済み） | Google が使えるかは **Supabase 側の `[auth.external.google] enabled`** で決まり、アプリからは判定できない。出し分けるなら `isSignupEnabled()` と同じ形の env フラグ（例 `NEXT_PUBLIC_GOOGLE_AUTH_ENABLED`）を足すことになる。**既定値をどちらにするかが本質**（既定 true は現状維持だがメール専用構成が壊れたまま、既定 false は未設定の本番で Google ログインが消える）。env を 1 つ増やす価値があるかを含めて未決 |

---

- 手順: [`.claude/skills/prototype/SKILL.md`](../../.claude/skills/prototype/SKILL.md)
- 背景: [prototype-first.md](./prototype-first.md)
- 現状の仕様（画面・データモデル・RLS）: **書きません。** 正は
  [`src/app/`](../../src/app/) と [`supabase/migrations/`](../../supabase/migrations/)、
  作業規約は [`CLAUDE.md`](../../CLAUDE.md)
